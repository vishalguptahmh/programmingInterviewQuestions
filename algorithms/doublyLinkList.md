```kotlin
class DNode(var value: Int) {
    var next: DNode? = null
    var prev: DNode? = null
}
class DoublyLinkList{
    // add at last
    // add at first
    // add at nth position
    // delete at last
    // delete at N
    // Reverse a linked list without recursion
    //Reverse a linked list with recursion


    // print elements
    // print at Nth element

}


```